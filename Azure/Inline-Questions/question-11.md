## MetaData
Question Type : Single Choice
Module : M3 - Elastic and Logstash

## Question
1. After configuring a Logstash pipeline, the destination Elasticsearch index still shows zero documents. Where should you look first?

## Options
Option 1 : The Kibana dashboard configuration, since a misconfigured dashboard can hide documents that already exist

Option 2 : The source system's replication factor, since low replication can prevent documents from being indexed

Option 3 : The Elasticsearch cluster's disk encryption settings, since encryption issues can silently block writes

Option 4 : The pipeline's input block, to confirm it is actually consuming from the intended source

## Answers
Option 4 : 2

## Number of Retries
1
