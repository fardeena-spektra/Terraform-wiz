## MetaData
Question Type : Single Choice
Module : M7 - Git and CI/CD (Azure DevOps)

## Question
3. A pipeline fails at the build stage with an error referencing a missing environment variable. What should you check?

## Options
Option 1 : Whether the repository needs to be re-cloned, since a stale clone can cause pipeline variables to go missing

Option 2 : Whether the YAML file has too many stages, since pipelines silently drop variables past a certain stage count

Option 3 : Whether the agent pool needs more storage, since low storage can prevent variables from loading correctly

Option 4 : Whether that variable (or secret) is actually defined for this pipeline/environment, since it may only exist in a different one

## Answers
Option 4 : 2

## Number of Retries
1
